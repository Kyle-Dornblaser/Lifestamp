<%= form_for(@lifestamp) do |f| %>
  <% if @lifestamp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lifestamp.errors.count, "error") %> prohibited this lifestamp from being saved:</h2>

      <ul>
      <% @lifestamp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :timestamp %><br>
    <%= f.datetime_select :timestamp %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.text_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
